import i18next from 'i18next';
import { translations } from 'locales/translations';

// export const VALIDATE_REQUIRE = i18next.t(translations.error.validate.require);
export const VALIDATE_REQUIRE = 'aaa';

export const SIDEBAR_DRAWER_MAX_WIDTH = 300;
export const SIDEBAR_DRAWER_MIN_WIDTH = 80;
